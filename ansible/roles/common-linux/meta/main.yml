---
# common-linux/meta/main.yml
# Role metadata and dependencies

galaxy_info:
  role_name: common-linux
  author: DarkArch0n
  description: >-
    Common Linux baseline configuration for all Linux VMs in the Crucible
    adversary research environment. Handles package management, user setup,
    SSH hardening, DNS (Dark Shrine), NTP, kernel hardening, UFW firewall,
    auditd/journald logging, and MOTD.
  license: MIT
  min_ansible_version: "2.14"

  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
    - name: Debian
      versions:
        - bullseye
        - bookworm

  galaxy_tags:
    - security
    - hardening
    - linux
    - research
    - malware
    - telemetry

dependencies: []
